// Supabase configuration.
//
// TODO: paste your project's values here (see config.example.js for details).
// These two values are safe to commit and make public — security comes from the
// Row-Level-Security policies in supabase/schema.sql plus email login. Never put the
// service_role key or database password in this file.
//
// Find these in: Supabase dashboard -> Project Settings -> API.

window.SUPABASE_CONFIG = {
  url: "https://YOUR-PROJECT-REF.supabase.co",
  anonKey: "YOUR-ANON-PUBLIC-KEY"
};
